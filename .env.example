# Used by the root-level Prisma CLI commands (migrate/generate/seed).
# Copy to .env and adjust if needed — must match apps/api/.env's DATABASE_URL.
# Special characters in the password (e.g. #, @, :) must be percent-encoded.
DATABASE_URL="mysql://root:password@localhost:3306/agentic_support"
